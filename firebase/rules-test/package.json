{
  "name": "youknow-rules-test",
  "private": true,
  "type": "module",
  "description": "Security rules tests for Firestore and Realtime Database, run against the emulators.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,database --project demo-youknow --config ../../firebase.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^15.31.0"
  }
}
